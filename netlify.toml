[build]
  base = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

# SPA fallback — client-side routing needs every path to resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
